services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: tasks
      POSTGRES_PASSWORD: tasks
      POSTGRES_DB: tasks
    ports: ["5434:5432"]
    volumes: [tasksdb:/var/lib/postgresql/data]
volumes:
  tasksdb:
